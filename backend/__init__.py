"""Top‑level package for HousingMatcher.

This file marks the ``housingmatcher`` directory as a Python package,
allowing its modules (such as ``main`` and ``utils``) to be imported
via dotted paths. Without this file, attempting to run ``uvicorn
housingmatcher.main:app`` would result in a ``ModuleNotFoundError``.

The version information and any package‑level variables can also be
defined here if needed in the future.
"""

__all__ = []